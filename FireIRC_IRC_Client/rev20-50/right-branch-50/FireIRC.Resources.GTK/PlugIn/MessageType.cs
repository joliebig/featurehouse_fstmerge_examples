namespace FireIRC.Resources.GTK.PlugIn
{
    public enum MessageType
    {
        OnText,
        OnAction,
        OnNotice,
    }
    public enum MessageZone
    {
        Channel,
        Query,
    }
}
