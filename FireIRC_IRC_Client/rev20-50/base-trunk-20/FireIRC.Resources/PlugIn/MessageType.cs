namespace OVT.FireIRC.Resources.PlugIn
{
    public enum MessageType
    {
        OnText,
        OnAction,
        OnNotice,
    }
    public enum MessageZone
    {
        Channel,
        Query,
    }
}
